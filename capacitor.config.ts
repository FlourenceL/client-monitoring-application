import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.happylink.services',
  appName: 'Happy Link Services',
  webDir: 'dist'
};

export default config;
