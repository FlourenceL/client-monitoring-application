export interface CreatePlanDTO {
    PlanName: string;
    Amount: number;
    IsActive: boolean;
}

export interface UpdatePlanDTO {
    PlanName: string;
    Amount: number;
    IsActive: boolean;
}