export interface CreateUserDTO {
  User: string
  Username: string;
  Password: string;
}

// export interface UpdateUserDTO { still not sure if needed
//   username?: string;
//   email?: string;
//   password?: string;
// }